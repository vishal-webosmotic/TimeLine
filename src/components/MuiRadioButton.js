import { Box, FormControl, FormLabel } from "@mui/material";
import React from "react";

const MuiRadioButton = () => {
  return (
    <Box>
      <FormControl>
        <FormLabel id="job"></FormLabel>
      </FormControl>
    </Box>
  );
};

export default MuiRadioButton;
